# CostGuard v17.0 Enterprise - Backend Dependencies
# Python 3.11 · Windows 11 (PowerShell/Docker Desktop) / macOS / Ubuntu

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.12.5
pydantic-settings==2.2.1
asyncpg==0.29.0
sqlalchemy==2.0.30
python-multipart==0.0.9
httpx==0.27.0
requests==2.32.5

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── ML / PyTorch ──────────────────────────────────────────────────────────────
# CPU-only torch (recommended on 16GB RAM Windows):
#   pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.3.0
torch-geometric==2.5.3

# ── Forecasting (CONSTRAINT-D: statsmodels, NOT Prophet) ──────────────────────
statsmodels==0.14.2
numpy==1.26.4
pandas==2.3.3

# ── Caching (CONSTRAINT-A: no Redis) ─────────────────────────────────────────
cachetools==5.3.3

# ── LLM & NL Queries ─────────────────────────────────────────────────────────
openai==1.30.0

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit==1.35.0
plotly==5.22.0
streamlit-autorefresh==1.0.1

# ── Monitoring ────────────────────────────────────────────────────────────────
prometheus-client==0.20.0

# ── Notifications ─────────────────────────────────────────────────────────────
slack-sdk==3.27.2
aiosmtplib==3.0.1
email-validator==2.1.1

# ── Rate Limiting ─────────────────────────────────────────────────────────────
slowapi==0.1.9

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
# httpx already listed above — no duplicate needed
pyyaml==6.0.1          # for docker-compose.yml healthcheck test

# ── Optional: Real Kafka (only when KAFKA_ENABLED=true, requires librdkafka) ──
# confluent-kafka==2.4.0

# ── NOT NEEDED (replaced by asyncio PCAM loop) ───────────────────────────────
# apache-flink==1.19.0
